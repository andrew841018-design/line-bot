# Install tooling in a separate venv; do not mix it into the launchd runtime.
# Semgrep 1.169.0 is intentionally omitted: it pins click~=8.1.8, which is
# affected by PYSEC-2026-2132. Re-add only after its upstream constraint moves.
bandit==1.9.4
click==8.3.3
cryptography==48.0.1
idna==3.15
msgpack==1.2.1
mypy==1.20.2
pip==26.1.2
pip-audit==2.10.0
PyJWT==2.13.0
pytest==9.0.3
ruff==0.15.12
setuptools==83.0.0
tomli==2.4.0
urllib3==2.7.0
